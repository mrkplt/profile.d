Cleaned a bunch of stuff out of tims profile I'll never use. Not all of it. Setup an entry that loads a scripts directory in the profile into the PATH so I have an easy spot to drop bash scripts. Add two useful scripts. Got rid of the pig. Check out Tim's profile for more details.
